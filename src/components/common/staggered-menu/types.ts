export type StaggeredMenuSocial = {
  label: string;
  href: string;
  icon: "mail" | "web";
};

export type StaggeredMenuItem = {
  label: string;
  ariaLabel: string;
  link: string;
};

export type MenuPosition = 'left' | 'right';
