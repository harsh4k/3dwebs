# Attachments

Obsidian drops pasted images here rather than the vault root, which would
otherwise litter the repo. Committed assets belong in `brand/` or
`public/` proper — this folder is for scratch captures and paste-ins.
